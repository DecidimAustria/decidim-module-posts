<style>
  <% reaction_types.each do |reaction_type| %>
    .feeds__feed_reactions_submenu button[data-reaction=<%= reaction_type.name %>]::after{
      content: '<%= reaction_icon(reaction_type.icon_name) %>';
    }
  <% end %>
</style>